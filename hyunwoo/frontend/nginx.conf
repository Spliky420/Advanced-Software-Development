server {
    listen 80;
    server_name _;

    # Serve the frontend files.
    root /usr/share/nginx/html;
    index index.html;

    # Return the main page for browser routes.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
